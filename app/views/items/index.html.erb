<h1>出品商品一覧</h1>
<ul>
  <% @items.each do |item| %>
    <!-- <li><a href="<%= item_path(item) %>"<%= item.id %>"><%= item.name %></a></li> -->
    <li><%= link_to item.name, item_path(item) %></li>
    <% end %>
</ul>
<!-- <a href="<%= new_item_path %>">出品する</a> -->
<%= link_to '出品する', new_item_path %>
